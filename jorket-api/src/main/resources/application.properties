spring.application.name=Jorket API
# --- Oracle datasource for app_dev ---
spring.datasource.url=jdbc:oracle:thin:@localhost:1521/XEPDB1
spring.datasource.username=app_dev
spring.datasource.password=password
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# JPA / Hibernate
 # change to 'validate' after schema is stable
spring.jpa.hibernate.ddl-auto=update   
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect

# Flyway
# enable later when you add migrations
spring.flyway.enabled= false            

spring.security.user.name=admin
spring.security.user.password=admin123
spring.security.user.roles=ADMIN

# === JWT CONFIG ===
jwt.secret=c03dee09fb0b9261f6bc046dec6e06c1


logging.level.root=INFO
logging.file.name=logs/jorket-api.log